# AI教育推進機構 学生部 公式サイト

このファイルは制作の常設ルールである。作業前に必ず読み、ここに反する出力をしない。
詳細仕様（head テンプレ・OGP・JSON-LD・SEO手順・デプロイ）は `SITE_SPEC.md` を参照する。

## このプロジェクトは何か
- AI教育推進機構 学生部の公式サイト。本部(`ai-ready-education.org`)の**下位ブランド**。
- 目的は**対外PR・実績紹介**。第一の読み手は提携先・一般、第二に参加希望の学生。
- 狙うトーンは「信頼できる先進性 + 人の温かさ」。冷たいテック感を避ける。

## スタック（厳守）
- **純粋な HTML / CSS / JS のみ。ビルド工程なし。フレームワーク不使用。**
- GitHub Pages で公開。1ファイル1ページ。共通の `css/style.css` と `js/main.js` を全ページで読む。
- 色・余白・角丸はすべて `:root` の CSS変数経由で使う。直値の手打ち禁止。

## 絶対にやらないこと（最重要）
「AIが量産したテンプレ」に見せないための禁止事項。違反は差し戻し対象。
- ❌ ピル型（`border-radius:100px`）のバッジ・ラベル。角丸は **4〜6px** に限定。
- ❌ グラデーション背景・放射状ブロブ・斜め clip-path。
- ❌ 常時表示のグラデ上線・光るドット・強い影・hover時の大きな拡大。
- ❌ 大文字・超ワイド letter-spacing ラベルの多用。
- ❌ **アクセント色を増やすこと。アクセントはコーラル一色のみ。**
- ✅ 代わりに: フラットな配色・控えめな角丸・タイポの強弱・コーラルの一点投入・ホバー時のみの控えめなアクセント線。

## デザイントークン
```css
:root {
  --bg-primary:    #f7f5ef;   /* 背景・温かいオフホワイト */
  --bg-secondary:  #efece4;   /* 交互セクション */
  --bg-card:       #ffffff;
  --ink:           #1a2740;   /* 見出し最濃 */
  --text-primary:  #22314f;   /* 強めの本文・小見出し（深インディゴ） */
  --text-secondary:#4a4f57;
  --text-muted:    #6b7078;
  --accent:        #e5613c;   /* コーラル・CTAとホバー線にだけ使う */
  --accent-dark:   #c24a28;   /* hover */
  --accent-soft:   rgba(229,97,60,0.08);
  --border:        rgba(26,39,64,0.10);
  --header-height: 72px;
  --section-pad:   72px 0;
  --max-width:     1200px;
  --radius:        6px;
  --radius-sm:     4px;
  --shadow:        0 2px 12px rgba(26,39,64,0.06);
  --shadow-hover:  0 6px 20px rgba(26,39,64,0.10);
}
```
- 見出し・英字: **Inter** / 本文(日本語): **Noto Sans JP**。Google Fonts を preconnect で読み込む。
- `.section-title` は `clamp(2rem,4.5vw,2.9rem)` / `font-weight:700` / `letter-spacing:-0.02em`。

## ページ構成
```
index.html       トップ（ヒーロー / 実績ハイライト / News / CTA）
about.html       私たちについて（理念・価値観3つ・設立背景・目指す姿）
activities.html  活動・実績【中心】（note / YouTube学習ガイド / 選書 / イベント）
members.html     体制・メンバー（運営学生 + 社会人/専門家の支援）
contact.html     お問い合わせ・参加（提携窓口 / メンバー募集）
```
- ナビ項目は全ページで統一: `活動・実績 / 私たちについて / 体制 / お問い合わせ`。
  ページ追加時は全ファイルのナビを更新する。
- お問い合わせのみコーラルのボタン扱いにして主CTAを際立たせる。
- 学習リソース（選書・学習導線）は v1 では `activities.html` 内のセクション。

## インタラクションの統一言語
「ラインがホバーで出現」をサイト全体の統一モチーフにする（色はコーラル）。
- カード: ホバーで上部コーラル線 + `translateY(-3px)` + 控えめな影。
- 項目: ホバーで左にコーラル縦線（`scaleY`で出現、レイアウトずれなし）。

## 文章・コンテンツの規約
- `lang="ja"`、セマンティック HTML、アクセシビリティに配慮。
- レポート的・論理的な文章は「だ・である」。UI文言は自然な常体。
- 事実は正確に。数値・日付・期間は**推測で書かず、ユーザーに確認**して確定する。
- 日付は絶対表記（「昨年」ではなく「2025.08」）。News・実績は新しい順。
- 煽り文は控えめに。英語ラベル + 日本語見出しの組み合わせを基調とする。
- 実績は誇張しない。活動は **「実施中／構想中」を明示**する（価値観=信頼の体現）。
- 「提携でできること」をトップと contact に最低3点、具体で置く（現状の空白を埋める）。

## 着手前にユーザーへ確認すること（勝手に仮定しない）
1. 公開先: `gakuseibu.ai-ready-education.org`（サブドメイン）か `github.io` のプロジェクトページか。
   → 絶対URL・`CNAME`・相対パス設計が変わる。
2. 提携先メリットの具体3点。
3. 学習リソースをサイト内公開するか外部リンクに留めるか。
4. ロゴ素材（本部ロゴ流用 or 派生）。

## 画像・デプロイの注意
- 画像は**ASCIIパスのみ**（日本語ファイル名はGitHub Pagesで表示されない）。
- iPhoneのHEICに注意（拡張子が`.jpg`でも中身がHEICだと表示されない→JPEG変換）。
- 更新は `git add → commit（末尾に Co-Authored-By）→ push`。push後1〜2分で反映。
- 詳細な head/OGP/canonical/JSON-LD/SEO手順は `SITE_SPEC.md` を参照。
